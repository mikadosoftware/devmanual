# Lots of things that need fixing

* install graphviz. But do so using a weaver wrapper, using fabirc.
  THis is because we want to swap out the underlying any time - so this is our way of keeong it simple and not learning ansible
  so we use ubunut apt get > this is basially a host service
  we want to validate somehow - test somehow what we expect to have????
  

* I need a single deployment location and a single venv location
  so that we can build sensible cmd lines for systemd / rc.d

* use weaver as a single CI, build, deploy, test, promote system
  THis gives us a clean single item to understand

  - target_build_dir =
